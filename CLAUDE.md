# business-flow-architect — セットアップ & 起動ガイド

このファイルは Claude Code が **このスキルディレクトリで作業を始めるとき** に自動で読まれるセットアップガイドです。利用者は「セットアップして」「初期化して」と投げるだけで、必要な準備が自動で完了します。

---

## 0. このスキルが何をするか(30 秒で)

会議の文字起こし(議事録 / Tactiq / TeamLog 等)を渡すと、5 分で以下 3 点が降りてくる:

- **業務記述書** — プロセス番号付き(P-XXX)で現状フローを分解 + 課題発生ポイント(C-XXX)を自動マーキング
- **業務フロー図(.drawio)** — drawio MCP でブラウザに即プレビュー
- **課題発生ポイント分析** — 「自社プロダクトで解決可能か」まで判定

商談ヒアリング / 社内の業務改善議論 / 経営会議 / 競合分析 / キックオフ など、文字起こしがある会議ならどれでも動く。

---

## 1. セットアップ(初回 1 回だけ)

利用者が以下のいずれかを投げたら、自動でセットアップを実行する。

- 「セットアップして」
- 「初期化して」
- 「business-flow-architect 入れて」
- 「業務フロースキルの準備して」

### セットアップ手順(Claude Code が自動実行)

#### Step 1: drawio MCP を `.mcp.json` に登録

プロジェクトルートの `.mcp.json` を確認:

- ファイルが無ければ `{ "mcpServers": {} }` の雛形を作る
- 既存 `mcpServers` に `drawio` キーが無ければ追加

追加する設定の例(実在パッケージ名は利用者の環境で確認):

```json
{
  "mcpServers": {
    "drawio": {
      "command": "npx",
      "args": ["-y", "@drawio/mcp"]
    }
  }
}
```

drawio MCP のパッケージ名は環境によって異なる(`drawio-mcp` / `@drawio/mcp-server` 等)。利用者に「お使いの drawio MCP パッケージ名を教えてください」とヒアリングして、適切な値に置き換える。

公式パッケージ名は **`@drawio/mcp`**(2026-05 時点)。バージョン違いがあれば利用者環境に合わせて読み替える。

#### Step 2: 出力先ディレクトリを決める

⚠️ **絶対必須**: ヒアリングを省略しない。利用者に必ず質問する。

```
業務フロー一式の出力先(ベース)はどこにしますか?
注: このリポジトリ自体は配布物のため、リポ内 outputs/ への生成は推奨しません。
    実成果物はリポジトリ外に出してください。

A. ./ai-outputs/business-flow/   — モノレポ型プロジェクト向け (推奨、ai-outputs/ 集約方式)
B. ./業務フロー/                  — プロジェクトルート直下、実務資産扱い
C. ./docs/business-flow/         — ドキュメント体系の中
D. .claude/skills/business-flow-architect/outputs/  ⚠️ 推奨しない (リポ内、配布物が汚れる)
E. カスタムパス — 利用者指定
```

選択結果を **プロジェクトルートの `.claude/business-flow-architect.local.yaml`** に書き込む(次回起動時にこの値を見て、再質問せずに済むようにする)。

⚠️ **`SKILL.md` 本体には絶対に書き込まない** — SKILL.md は配布物(利用者間で共有)なので、固有プロジェクトの出力先を埋め込むと汚染される。**プロジェクトローカルな `.local.yaml` に分離する**のが鉄則。

`.claude/business-flow-architect.local.yaml` の中身(AI が書く):

```yaml
# business-flow-architect プロジェクトローカル設定
# このファイルは .gitignore で除外することを推奨

output:
  path: ./ai-outputs/business-flow/   # 利用者が選んだパス
  configured_at: "YYYY-MM-DD"
```

利用者の `.gitignore` に以下が無ければ追加するよう案内:

```
.claude/business-flow-architect.local.yaml
```

選ばれたベースディレクトリが無ければ `mkdir -p` で作成。

**生成時の実際の構造**: ベース出力先の下に、各ヒアリングごとに [プロセス名] のサブディレクトリを自動で作って 3 ファイルを束ねる。

```
{選ばれたベース出力先}/
├── 経費精算プロセス/
│   ├── 経費精算プロセス_業務記述書_v1.0_20260528.md
│   ├── 経費精算プロセス_業務フロー図_v1.0_20260528.drawio
│   └── 経費精算プロセス_課題発生ポイント分析_v1.0_20260528.md
├── メンバー状況把握/
│   ├── メンバー状況把握_業務記述書_v1.0_20260601.md
│   └── ...
└── ...
```

詳細は `SKILL.md` 末尾「ファイル名規約 + ディレクトリ構造」を参照。

#### Step 3: 動作確認

セットアップ完了後、以下を Claude Code が報告:

```
✅ セットアップ完了

- drawio MCP: 登録済み (.mcp.json に追記)
- 出力先: [選択されたパス]
- 起動キーワード: 「業務フロー作って」「会議を drawio にして」「ヒアリングを構造化して」など

試しに以下のように使えます:
  「@hearings/サンプル.md の業務フロー一式を作って」

Claude Code を一度再起動してから、上記のキーワードで起動してみてください。
```

#### Step 4: 起動キーワードのカスタマイズ提案

「あなたの語彙に合わせて起動キーワードを追加できます。例えば『うちの会議を整理して』『チームの業務を図にして』など、自分が普段使う言葉があれば教えてください。SKILL.md に追加します」と提案。

利用者が言葉を渡してきたら `SKILL.md` の `triggers` セクションに追記。

---

## 2. 通常起動(セットアップ後の毎回)

利用者が以下のいずれかを投げたら、`SKILL.md` の実行手順を読んで業務フロー一式を生成する。

- 「業務フロー作って」「会議を drawio にして」「ヒアリングを構造化して」「課題発生ポイント抽出」 …(`SKILL.md` の `triggers` 参照)

### 実行の流れ

`SKILL.md` の Step 1〜6 をそのまま実行。
出力先は本ファイル Step 2 で決めたパスを使う(`SKILL.md` 末尾の「出力先設定」セクションを参照)。

---

## 3. メンテナンス

### スキルの更新

利用者が「スキル更新して」と投げたら:

```bash
cd .claude/skills/business-flow-architect
git pull
```

を実行して、最新版を取得する。カスタマイズしたファイル(`SKILL.md` の triggers 等)があれば、git の conflict 表示を見せて利用者に判断を仰ぐ。

### トラブルシューティング

`docs/troubleshooting.md` を参照(未作成の場合は、利用者からの具体的なエラーを聞いてから判断)。

---

## 4. このファイル自体について

このファイル(`CLAUDE.md`)は、業務フロー一式の生成では参照されません。**セットアップとメンテナンスのときだけ**読まれます。
生成本体のロジックは `SKILL.md` と `prompts/base-prompt.md` にあります。
