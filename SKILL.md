---
name: business-flow-architect
description: 会議の文字起こし(議事録/Tactiq/TeamLog 等)から、業務記述書 + drawio 業務フロー図 + 課題発生ポイント分析 の 3 点セットを 5 分で生成する。商談ヒアリング・社内の業務改善議論・経営会議・競合分析・キックオフなど、文字起こしがある会議ならどれでも使える。Claude Code + drawio MCP 前提。

triggers:
  # 通常起動 (業務フロー生成)
  - 業務フロー作って
  - 業務フローを作って
  - 業務フロー一式
  - 会議を drawio にして
  - 会議を業務フロー図にして
  - 経営会議を drawio に
  - ヒアリングを構造化して
  - ヒアリングを drawio にして
  - 課題発生ポイント抽出
  - 業務記述書を作って
  - 業務記述書 + drawio
  - 戦略図にして
  - 戦略図を作って
  - drawio で構造化
  - drawio フロー図
  - 商談を整理して
  - 議事録を業務フローに
  # セットアップ起動 (初回 1 回だけ、CLAUDE.md を読んで自動設定)
  - セットアップして
  - 初期化して
  - business-flow-architect 入れて
  - 業務フロースキルの準備して
  # メンテナンス
  - スキル更新して
  - business-flow-architect 更新

invocation_examples:
  - "@meeting-2026-05-15.md の業務フロー一式を作って"
  - "先週の経営会議を drawio にして"
  - "ヒアリング 5 件をまとめて共通する課題発生ポイントを抽出して"
  - "競合分析の議論を戦略図にして"
  - "business-flow-architect をセットアップして"
---

# business-flow-architect

会議の文字起こしから、業務フロー + 課題構造 + 戦略図 を一気通貫で生成する Claude Code Skill。

## このスキルの使い方

ユーザーが以下のいずれかの言葉で起動したら、次の手順で 3 つの成果物を生成する。

### 必要な事前条件

- Claude Code がインストールされている
- drawio MCP が `.mcp.json` に登録されている(プロジェクトルート)
- 会議の文字起こしファイル(`.md` または `.txt`)がある

### ⚠️ 最重要: リファレンス実例を必ず参照する

**実行前に、必ず `examples/経費精算プロセス/` の以下 3 ファイルを `Read` で読み込んでください。**

- `examples/経費精算プロセス/output/経費精算プロセス_業務記述書_v1.0_20260527.md`
- `examples/経費精算プロセス/output/経費精算プロセス_業務フロー図_v1.0_20260527.drawio`
- `examples/経費精算プロセス/output/経費精算プロセス_課題発生ポイント分析_v1.0_20260527.md`

これらは AI が出力するべき **「最低限の粒度・密度の基準」** です。新しいヒアリングを処理するときは、このサンプルを参照して以下を真似てください:

- 業務記述書の §2 プロセス詳細表の埋め方(活動内容の具体性、改行 `<br>` の使い方、複数行に渡る記述)
- 各 P-XXX 備考欄の課題マーキング `⚠️ C-XXX [課題内容]` の書き方
- §5 課題発生ポイント分析の根本原因深掘り(顧客の言葉の逐語引用 + 文脈補足)
- drawio の要素配置(どのレーンに、どの y 座標で配置するか)
- 課題発生ポイント分析(独立 .md)のエグゼクティブサマリー + 顧客像推測の書き方

実ヒアリングが情報量少なくても、リファレンスの構造は守る(空欄を作らず、推測でも仮置きする)。

### 実行手順

#### ⚠️ Step 0: 出力先の確認(初回 + 不明時は必ず実施)

スキルが起動したら、**まず出力先が確定しているかチェック** する。

**チェック方法**:

1. ユーザーが args で出力先を明示している?(例: 「出力先は ./ai-outputs/business-flow/ に」)→ 明示先を使う
2. プロジェクトルートに **`.claude/business-flow-architect.local.yaml`** があるか? → あればその `output.path` を使う(`Read` で読む)
3. 上記いずれも無い場合 → **必ず利用者に質問する** (推測で勝手に決めない)、決まったら **`.claude/business-flow-architect.local.yaml` に保存** して次回以降は再質問しない:

```
業務フロー一式の出力先はどこにしますか?

候補:
A. ./ai-outputs/business-flow/   — モノレポ型プロジェクト向け(`ai-outputs/` に集約する方式)
B. ./業務フロー/                  — プロジェクトルート直下、実務資産扱い
C. ./docs/business-flow/         — ドキュメント体系の中に組み込む
D. このスキルディレクトリ内 .claude/skills/business-flow-architect/outputs/   ← ⚠️ 推奨しない (リポは配布物、利用者の実成果物は別の場所に出すべき)
E. カスタムパス(指定してください)
```

⚠️ **リポジトリ内 (`.claude/skills/business-flow-architect/outputs/`) は基本的に推奨しない**。本リポは利用者間で共有される配布物のため、利用者の実成果物が混ざるのは適切でない(リポを git pull したり差し替えるときに混乱の元になる)。

回答を得たら、選ばれたパスをこの実行のスコープで保持し、Step 2 以降で使う。

#### Step 1: 文字起こしを読み込む

ユーザーが渡したファイル(または直前のメッセージで指定されたパス)を `Read` ツールで読み込む。
複数件指定された場合は順番に読み込み、共通要素も後で抽出する。

#### Step 2: 出力ディレクトリを作る + 業務記述書を生成

まず **[プロセス名] のサブディレクトリを作成** する:

```bash
mkdir -p "{出力先}/[プロセス名]/"
```

例: 出力先が `./outputs/` で、プロセス名が「経費精算プロセス」なら、`./outputs/経費精算プロセス/` を作る。

そのディレクトリ内に業務記述書を出力:

`{出力先}/[プロセス名]/[プロセス名]_業務記述書_v1.0_$(date +%Y%m%d).md`

例: `./outputs/経費精算プロセス/経費精算プロセス_業務記述書_v1.0_20260528.md`

含める要素:

- **1. プロセス概要** — プロセス名 / 目的 / 範囲(開始点・終了点) / 関連部門
- **2. プロセス詳細** — 表形式で `P-001`, `P-002`, ... を時系列順に
  - 列: プロセス番号 / プロセス名 / 実施者 / 活動内容 / 使用証憑 / 作成証憑 / ファイル番号 / システム連携 / 前工程依存 / 後工程依存 / 備考
  - 備考欄に **⚠️ 課題発生ポイント** を `C-XXX` 番号付きでマーキング
- **3. ファイル・データ一覧** — `F-001`, `F-002`, ... を使用順序で
- **4. システム連携** — `S-001`, `S-002`, ... を実行順序で
- **5. 課題発生ポイント分析** — `C-XXX` ごとに以下を整理
  - プロセス番号 / 課題内容 / 根本原因 / 自社プロダクトで解決可能か(✅ 可能 / ⚠️ 間接的 / ❌ 不可)
- **6. 時系列チェック項目**
- **7. 改訂履歴**

詳細フォーマットは `templates/業務記述書テンプレート.md` を参照。

採番ルール:

- `P-XXX`: 時系列順
- `F-XXX`: 使用順序、同一ファイルが複数で使われるなら最初の番号を採用
- `S-XXX`: 実行順序
- `C-XXX`: 課題が見えた順序

時系列の原則(絶対):

- ✅ 開始点から終了点まで上から下への一貫した流れ
- ❌ 上から下に反する配置は禁止

#### Step 3: 業務フロー図 (drawio XML) を生成

`{出力先}/[プロセス名]/[プロセス名]_業務フロー図_v1.0_$(date +%Y%m%d).drawio` に保存(Step 2 で作成したサブディレクトリ内)。

XML 骨格は `templates/業務フロー図テンプレート.drawio` を参照。
スイムレーン 9 種・図形要素(楕円/角丸四角/ひし形/四角/付箋/吹き出し)・フロー線(実線/破線)の規約は `prompts/base-prompt.md` の §② を遵守する。

特に重要:

- **課題発生ポイント `C-XXX` は吹き出し型(shape=callout; strokeColor=#D32F2F)** で該当 `P-XXX` の近くに配置
- 業務記述書 §2 の `P-XXX` / §3 の `F-XXX` / §4 の `S-XXX` / §5 の `C-XXX` が drawio 内と完全に一致

#### Step 4: drawio MCP でブラウザプレビュー

drawio MCP の `open_drawio_xml` ツールに、生成した XML 文字列を渡す。
ブラウザで drawio.com が自動で開いて、図がプレビューされる。

XML エラーで開かない場合:

- `&#xa;` を改行に使う
- `<` `>` `&` `"` は実体参照に
- 一度 Write でファイルに保存 → Read で再読 → MCP に渡す

#### Step 5: 課題発生ポイント分析 (独立 Markdown) を生成

`{出力先}/[プロセス名]/[プロセス名]_課題発生ポイント分析_v1.0_$(date +%Y%m%d).md` として、同じサブディレクトリ内に独立保存。

含める要素:

- 課題サマリー(`C-XXX` 一覧、深刻度判定)
- 根本原因の分類(人 / プロセス / システム / 文化)
- 自社プロダクトでの解決可能性マトリクス
- 次の検証アクション提案

#### Step 6: 整合性チェック + 報告

最後に、以下をユーザーに報告:

1. 生成した 3 ファイルのパス
2. `P-XXX` / `F-XXX` / `S-XXX` / `C-XXX` が業務記述書と drawio で一致しているかのチェック結果
3. drawio MCP のプレビュー URL(自動で開かれているはず)

## 起動キーワードの柔軟性

ユーザーが以下のような揺らぎを伴って起動しても反応する:

| ユーザーの言葉 | 反応 |
|---|---|
| 「業務フロー作って」 | フル 3 点セット生成 |
| 「会議を drawio にして」 | フル 3 点セット生成 |
| 「ヒアリングを構造化して」 | フル 3 点セット生成 |
| 「課題発生ポイント抽出だけ」 | §5 課題発生ポイント分析のみ生成 |
| 「業務記述書だけ」 | §1-7 の業務記述書のみ生成 |
| 「drawio だけ作り直して」 | 業務フロー図 .drawio のみ再生成 |
| 「先月の会議 5 件まとめて」 | 複数ヒアリングの共通課題構造を抽出 |

## カスタマイズ方法

利用者は `SKILL.md` の `triggers` セクションに **自分の好きな起動キーワード** を追加できます。
例えば「うちの会議を整理して」を追加すれば、Claude Code はそれにも反応します。

詳細は `docs/customize.md` を参照。

---

## 出力先設定

業務フロー一式の生成物(`.md` / `.drawio`)の出力先を、ここで指定します。

⚠️ **重要**: 利用者プロジェクトの実成果物は、**このリポジトリ外**(例: `./ai-outputs/business-flow/` や `./業務フロー/`)に出すこと。リポ内 `outputs/` は **リポ同梱の例 + 自己テスト専用**(利用者の実成果物を置く場所ではない)。

初回起動時(`SKILL.md` Step 0)で利用者に質問して、選ばれたパスをここに記録します。

```yaml
output:
  # 出力先 (絶対パス or プロジェクトルートからの相対パス)
  # ⚠️ 共有 SKILL.md には固有プロジェクトのパスを書かない。
  # 実際の利用者設定は .claude/business-flow-architect.local.yaml に保存。
  path: (未設定 — 初回起動時に Step 0 で利用者に質問)

  # 候補一覧
  alternatives:
    - "./ai-outputs/business-flow/"                   # A. モノレポ型プロジェクト向け
    - "./業務フロー/"                                 # B. プロジェクトルート直下 (実務資産扱い)
    - "./docs/business-flow/"                         # C. ドキュメント体系内
    - ".claude/skills/business-flow-architect/outputs/"  # D. リポ内 ⚠️ 推奨しない
    - "(カスタム)"                                    # E. 利用者指定パス
```

### ファイル名規約 + ディレクトリ構造

1 回の生成で 3 ファイル(.md × 2 + .drawio × 1)を作るので、必ず **[プロセス名] のサブディレクトリ** を作って束ねる。バラバラに置かない。

```
{出力先}/
├── [プロセス名 A]/
│   ├── [プロセス名 A]_業務記述書_v1.0_[YYYYMMDD].md
│   ├── [プロセス名 A]_業務フロー図_v1.0_[YYYYMMDD].drawio
│   └── [プロセス名 A]_課題発生ポイント分析_v1.0_[YYYYMMDD].md
├── [プロセス名 B]/
│   ├── [プロセス名 B]_業務記述書_v1.0_[YYYYMMDD].md
│   ├── [プロセス名 B]_業務フロー図_v1.0_[YYYYMMDD].drawio
│   └── [プロセス名 B]_課題発生ポイント分析_v1.0_[YYYYMMDD].md
└── ...
```

例(出力先 = `./outputs/`、プロセス名 = `経費精算プロセス`):

```
./outputs/
└── 経費精算プロセス/
    ├── 経費精算プロセス_業務記述書_v1.0_20260528.md
    ├── 経費精算プロセス_業務フロー図_v1.0_20260528.drawio
    └── 経費精算プロセス_課題発生ポイント分析_v1.0_20260528.md
```

複数回ヒアリングする場合(同じ会社の別プロセス、別会社等)、それぞれ別のサブディレクトリで束ねる。**1 ヒアリング = 1 サブディレクトリ = 3 ファイル** が原則。

バージョン違い(v1.0 → v1.1 など)は **同じサブディレクトリ内** に並べる(資産が散らばらないように):

```
./outputs/経費精算プロセス/
├── 経費精算プロセス_業務記述書_v1.0_20260528.md
├── 経費精算プロセス_業務記述書_v1.1_20260605.md  ← 更新版
├── 経費精算プロセス_業務フロー図_v1.0_20260528.drawio
└── ...
```

### 出力先の変更

「出力先を ○○ に変えて」と利用者が投げたら、上記 `output.path` を書き換えて確認応答する。
変更前に旧パスの生成物がある場合は、移動するか残すか利用者に確認する。

### Git 管理

スキルディレクトリ内の `outputs/`(デフォルト)は `.gitignore` で除外されている。
他のディレクトリを指定した場合、利用者プロジェクト側の `.gitignore` で適切に管理する。

